import { StyleSheet } from "react-native";

const StyleSIImage = StyleSheet.create({
  viewContainer: {
    alignItems: "center",
    justifyContent: "center",
  },
  imageContainer: {
    width: "100%",
    height: 253,
  },
});
export default StyleSIImage;
