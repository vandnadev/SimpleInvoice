
import { StyleSheet } from "react-native";
import { COLORS } from "../../constant/Color";

const SIextStyle = StyleSheet.create({
  viewContainer: {},
  labelText: {
    fontSize: 14,
    color: COLORS.BLACK[100],
  },
});
export default SIextStyle;
